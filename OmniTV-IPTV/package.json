{
  "name": "omnitv-iptv",
  "version": "1.0.0",
  "private": true,
  "description": "Monorepo OmniTV IPTV apps for Tizen, webOS, and Roku with shared TypeScript core.",
  "scripts": {
    "build": "npm run build -w packages/core && npm run build -w packages/ui && npm run build -w apps/tizen-web && npm run build -w apps/webos-web",
    "dev:tizen": "npm run dev -w apps/tizen-web",
    "dev:webos": "npm run dev -w apps/webos-web",
    "zip:roku": "node tools/roku-zip.mjs"
  },
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "engines": {
    "node": ">=18.0.0"
  },
  "devDependencies": {
    "archiver": "5.3.1",
    "typescript": "5.4.5"
  }
}
